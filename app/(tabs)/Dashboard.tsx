import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";
import { ProgressBar } from "@/components/ProgressBar";

export default function DashboardScreen() {
  return (
    <SafeAreaView>
      <Text>Dashboard123</Text>
    </SafeAreaView>
  );
}
